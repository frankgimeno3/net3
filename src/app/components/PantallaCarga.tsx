import React, { FC } from 'react';

interface PantallaCargaProps {
  
}

const PantallaCarga: FC<PantallaCargaProps> = ({ }) => {
  return (
    <div>PantallaCarga</div>
    // Hacer una pantalla de carga como un 3 que se carga
  );
};

export default PantallaCarga;